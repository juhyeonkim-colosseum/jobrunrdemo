# 프로젝트 최상위 설정임을 표시 (상위 디렉터리 탐색 중단)
config.stopBubbling = true

# @CustomLog 이 생성할 로거 선언 매핑.
# slf4j Logger 타입의 필드를 DashboardLoggerFactory.getLogger(<클래스>.class) 로 초기화한다.
lombok.log.custom.declaration = org.slf4j.Logger com.example.jobrunrdemo.DashboardLoggerFactory.getLogger(TYPE)
